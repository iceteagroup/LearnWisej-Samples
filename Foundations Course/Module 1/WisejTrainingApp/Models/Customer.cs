namespace WisejTrainingApp.Models
{
    /// <summary>
    /// The second data class the lesson names under Models/. Module 1 only needs it to make the
    /// folder real; later modules give it a service and a screen of its own.
    /// </summary>
    public class Customer
    {
        public int Id { get; set; }

        public string Name { get; set; } = "";
    }
}
