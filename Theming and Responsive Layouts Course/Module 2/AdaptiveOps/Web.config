<?xml version="1.0"?>
<configuration>
	<appSettings>
		<add key="Wisej.LicenseKey" value=""/>
		<!-- Same startup theme as Default.json "theme": the file Themes/AdaptiveOps.theme (name only, no path, no extension). -->
		<add key="Wisej.DefaultTheme" value="AdaptiveOps"/>
	</appSettings>
</configuration>
