using Wisej.Web;

namespace AdaptiveOps
{
    /// <summary>
    /// The scaffold's single screen, so a fresh copy of `_template` builds and runs before the
    /// module's own screen exists. Rename it to the screen the module names (and change the line in
    /// <see cref="Program"/>), or delete it once that screen is in place.
    /// </summary>
    public partial class MainPage : Page
    {
        public MainPage()
        {
            InitializeComponent();
        }
    }
}
