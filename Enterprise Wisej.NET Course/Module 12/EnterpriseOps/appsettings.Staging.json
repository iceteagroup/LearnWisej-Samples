{
  "//": "Staging (the runbook's 'test' environment). The test database uses integrated security, so the connection string is not a secret; the IdP client secret IS one and is missing on purpose — it arrives as EnterpriseOps__IdentityProvider__ClientSecret.",
  "EnterpriseOps": {
    "ConnectionString": "Server=sql-staging;Database=EnterpriseOps_Test;Integrated Security=true",
    "Logging": {
      "Level": "Information"
    },
    "UploadLimitMb": 50,
    "FeatureFlags": "release-set",
    "IdentityProvider": {
      "Authority": "https://login.staging.contoso-fieldops.example/oauth2",
      "ClientId": "enterpriseops-staging"
    },
    "AllowedHosts": "staging.enterpriseops.example"
  }
}
