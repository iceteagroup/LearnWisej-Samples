namespace EnterpriseOps.Security
{
    /// <summary>The three roles the course uses. Permissions are decided by policies, never by the UI.</summary>
    public enum UserRole
    {
        Technician,
        Manager,
        Admin
    }
}
