# ValidationClinic starter

This is the working Module 1 baseline, on port 5900. Copy this folder to start another independent snapshot.
Open `ValidationClinic.slnx`, or run `dotnet run -f net10.0 --urls http://localhost:5900` from `ValidationClinic`.
Read [COOKBOOK.md](COOKBOOK.md) before extending the form, binding or grid behavior.
